<%= form_with(model: plataform) do |form| %>
  <% if plataform.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(plataform.errors.count, "error") %> prohibited this plataform from being saved:</h2>

      <ul>
        <% plataform.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :active, style: "display: block" %>
    <%= form.check_box :active %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
